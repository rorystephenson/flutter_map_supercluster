name: flutter_map_supercluster_example
description: Example for Flutter Map Supercluster

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  flutter_map: ^6.0.0-dev.1
  flutter_map_animations: ^0.4.1
  flutter_map_marker_popup: ^5.3.0-dev.1
  flutter_map_supercluster:
    path: ../
  latlong2: ^0.9.0

dev_dependencies:
  flutter_lints: ^2.0.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  fonts:
    - family: AccurateMapIcon
      fonts:
        - asset: fonts/AccurateMapIcon.ttf

dependency_overrides:
  flutter_map_animations:
    git:
      url: https://github.com/rorystephenson/flutter_map_animations
      ref: flutter-map-v6
